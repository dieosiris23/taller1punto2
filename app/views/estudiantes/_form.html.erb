<%= form_with(model: estudiante, local: true) do |form| %>
  <% if estudiante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estudiante.errors.count, "error") %> prohibited this estudiante from being saved:</h2>

      <ul>
      <% estudiante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :estudiante_nombre %>
  </div>

  <div class="field">
    <%= form.label :birthday %>
    <%= form.date_select :birthday, id: :estudiante_birthday %>
  </div>

  <div class="field">
    <%= form.label :mail %>
    <%= form.text_field :mail, id: :estudiante_mail %>
  </div>

  <div class="field">
    <%= form.label :papa %>
    <%= form.text_field :papa, id: :estudiante_papa %>
  </div>

  <div class="field">
    <%= form.label :creditos_inscritos %>
    <%= form.text_field :creditos_inscritos, id: :estudiante_creditos_inscritos %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
